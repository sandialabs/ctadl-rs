[workspace]
resolver = "3"
members = ["ctadl-ascent", "ctadl-dynamic", "ctadl-flowy", "ctadl-ir", "pcode-reader", "sarif-util", "trie", "tailshare", "immortal", "xtask"]
# In-tree path dependencies become workspace members, so their bins would be
# installed into the distro by `nix build .#packages.default`. dex-reader and
# jvm-reader are libraries used by ctadl-ascent but their standalone bins must
# not ship; excluding them drops their membership (and bins) while they are
# still built as ordinary path-dependency libraries.
exclude = ["dex-reader", "jvm-reader"]

[workspace.package]
version = "0.1.0"

# Section defining rustc compiler lint rules for the workspace
[workspace.lints.rust]

# Fail on violations of the `rust-2018-idioms` lint group
rust-2018-idioms = "deny"

# Section defining Clippy lint rules for the workspace
[workspace.lints.clippy]

# Require a "SAFETY" comment preceding any unsafe code
undocumented_unsafe_blocks = "deny"

# Ignore a couple Clippy warnings that aren't important IMO
type_complexity = "allow"
too_many_arguments = "allow"
large_enum_variant = "allow"
result_large_err = "allow"
# Our Ascent code uses _ as a name all the time
just_underscores_and_digits = "allow"

[profile.release]
debug = true
