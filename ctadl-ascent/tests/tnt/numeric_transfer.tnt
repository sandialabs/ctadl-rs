
// Test numeric field access in summary requirements

def Transfer(a, b): 0
where summaries [a.[1].deref <- b]
{
start:
    a.[1].deref = b;
    return;
}

def Main() {
start:
  s = source(Test);
  Transfer(x.[1], s);
  sink(x.[2].deref, Test);
  return;
}
