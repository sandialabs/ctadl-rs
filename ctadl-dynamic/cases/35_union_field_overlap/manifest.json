{
  "name": "union_field_overlap",
  "description": "union aliasing -- u.a = src then read u.b carries taint (shared storage). [RESOLVED: frontend collapses union members to a shared field; plain OK regression case]",
  "label": "Test",
  "expect_flow": true
}
