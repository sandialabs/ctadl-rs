{
  "name": "label_empty_statement",
  "description": "label on an empty statement (done: ;) after a goto -- frontend can't ingest the null-statement label body (ERR 78); DFSan sees the flow [RESOLVED: frontend now skips the empty `;`; plain OK regression case]",
  "label": "Test",
  "expect_flow": true
}
