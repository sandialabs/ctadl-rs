{
  "name": "aggregate_initializer",
  "description": "aggregate initializer list int a[2] = { s, 0 } -- frontend can't ingest initializer_list (ERR 78); DFSan sees the flow [RESOLVED: frontend now lowers this; plain OK regression case]",
  "label": "Test",
  "expect_flow": true
}
