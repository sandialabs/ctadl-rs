{
  "name": "aggregate_initializer",
  "description": "aggregate initializer list int a[2] = { s, 0 } -- frontend can't ingest initializer_list (ERR 78); DFSan sees the flow",
  "label": "Test",
  "expect_flow": true,
  "known_frontend_gap": "initializer_list"
}
