{
  "name": "addr_of_local_alias",
  "description": "F3 (soundness): writing through a local's address (int *p = &x; *p = src) does not taint x -- CTADL drops the local-alias write-back; DFSan sees the flow. Contrast case 14 (read through alias) and 15 (*out=src through a pointer param), both of which work.",
  "label": "Test",
  "expect_flow": true,
  "known_gap": "F3"
}
