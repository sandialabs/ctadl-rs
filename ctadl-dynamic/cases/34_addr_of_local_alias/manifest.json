{
  "name": "addr_of_local_alias",
  "description": "writing through a local's address (int *p = &x; *p = src) taints x -- contrast case 14 (read through alias) and 15 (*out=src through a pointer param). [RESOLVED: frontend resolves a same-block `*p` to its address-of pointee; plain OK regression case]",
  "label": "Test",
  "expect_flow": true
}
