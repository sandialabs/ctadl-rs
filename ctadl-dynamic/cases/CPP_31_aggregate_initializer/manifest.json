{
  "name": "CPP_31_aggregate_initializer",
  "description": "C++: aggregate initializer list int a[2] = { s, 0 }: desugared element-wise by the shared core, so the tainted element lands on a[0] -- s=flow d=flow (gap closed by spec 019)",
  "label": "Test",
  "expect_flow": true
}
