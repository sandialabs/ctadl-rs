{
  "name": "nonconstant_subscript",
  "description": "a[n] with n==0 at runtime writes a[0]; a non-constant subscript may-aliases every concrete index while two CONSTANT indices stay disjoint. [RESOLVED: [_elem_] may-aliases [N] in the taint-index path matcher; plain OK regression case]",
  "label": "Test",
  "expect_flow": true
}
