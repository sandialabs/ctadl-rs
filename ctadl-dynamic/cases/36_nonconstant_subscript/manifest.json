{
  "name": "nonconstant_subscript",
  "description": "F5 (soundness): a[n] with n==0 at runtime writes a[0], but CTADL lowers a[n] to a distinct [_elem_] field symbol vs a[0] -> [0] and treats them as disjoint, dropping the may-alias flow; DFSan sees it. Contrast the correct precision of distinguishing two CONSTANT indices.",
  "label": "Test",
  "expect_flow": true,
  "known_gap": "F5"
}
