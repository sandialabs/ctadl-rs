[package]
name = "immortal"
version = "0.1.0"
edition = "2024"

[dependencies]
parking_lot = "0.12"
hashbrown = "0.15"
rustc-hash = "2"
serde = { version = "1.0", features = ["derive"], optional = true }

[features]
serde = ["dep:serde"]
