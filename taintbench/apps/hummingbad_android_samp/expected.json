{
  "_comment": "Baseline snapshot: TaintBench finding IDs ctadl currently detects via a connected source->sink path. Both flows (Cursor.getString -> SQLiteDatabase.insert/update) connect. They did not under the datalog closure engine; the demand-driven search regime (the default since the query-engine switch) finds them. See ../../README.md.",
  "matched_finding_ids": [1, 2]
}
